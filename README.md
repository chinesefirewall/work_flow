# work_flow
A small, Windows-friendly Python script that types text or simulates key presses every N seconds for a configurable duration, then exits. Works with Google Sheets, Excel, Notepad, etc. (active window must be focused).
